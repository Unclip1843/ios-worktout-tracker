# WorkoutTracker — Full (iOS 17+, SwiftUI + SwiftData + Charts)

Minimal workflow, all requested features:
- Track strength sets and cardio sessions (time/distance)
- Per-day totals and PRs (best set, best day)
- Analyze charts: strength totals, cardio distance, weight line chart
- Journal with text + photos (saved to app Documents)
- Weight logging with time (displayed + charted)
- Swipe left/right to change day on Track & Journal
- Units: mi/km and lb/kg (distance stored as km, weight stored as kg)

Open `WorkoutTracker.xcodeproj` in Xcode 15+, run on iOS 17+.
